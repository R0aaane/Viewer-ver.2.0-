import 'package:flutter/material.dart';

import '../../app/app_theme.dart' as pdf_viewer_theme;

ThemeData buildAppTheme({required bool useBlackBackground}) {
  return pdf_viewer_theme.buildAppTheme();
}
